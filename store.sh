# terrain code:store clearing-house --signer validator --network localterra 
# terrain code:store collateral-vault --signer validator --network localterra --no-rebuild
# terrain code:store insurance-vault --signer validator --network localterra --no-rebuild
# terrain code:store historical-store --signer validator --network localterra --no-rebuild



terrain code:store clearing-house --signer admin --network testnet  
# terrain code:store collateral-vault --signer admin --network testnet 
# terrain code:store insurance-vault --signer admin --network testnet 
# terrain code:store historical-store --signer admin --network testnet 

# terrain code:store oracle --signer admin --network testnet --no-rebuild